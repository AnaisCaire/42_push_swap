#include <stdio.h>

typedef struct s_list
{
	int num;
	struct s_list *next;
} t_list;

/* 
1. declare 2 pointers to stack a and b 
2. control errors
3. ft_split to organise integers = declare them
4. stack_init
5. sort stack algorithm:
	a. tiny_sort
	b. push_swap
*/

int	main(int argc, char **argv)
{
	if (argc == 2)
	{

	}
	printf("\n");
}